export const environment = {
    production: true,
    name: "prod",
    useScheme: true,
    useHost: true,
    usePort: true,
    serviceScheme: 'http',
    serviceHost: 'localhost',
    servicePort: 3000,
    apiContextRoot: '/'
};
